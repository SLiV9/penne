
fn sum(x: i32, y: i32) -> i32
{
	return:
	x + y
}

fn main()
{
	var a = 3;
	var b: i32 = 7;
	var x = sum(a, b);
	var y = sum(b, a);
	if x == y
	{
		if x == a + b
			goto done;
		else
			goto hell;
	}
	hell:
	{
		loop;
	}
	done:
}
