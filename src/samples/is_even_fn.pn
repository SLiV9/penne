fn is_even(x: i32) -> bool
{
	var is_even = false;
	{
		if x == 0
		{
			is_even = true;
			goto return;
		}
		else if x == 1
			goto return;
		x = x - 2;
		loop;
	}
	return:
	is_even
}

fn main()
{
	var x = 33;

	return:
	is_even(x)
}
