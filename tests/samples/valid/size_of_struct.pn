word64 Position
{
	x: i32,
	y: i32,
}

struct Foo
{
	a: u8,
	x: i32,
}

struct Bar
{
	x: i128,
	foo: Foo,
	pos: Position,
}

struct Baz
{
	a: u8,
	x: i128,
	foo: Foo,
}

struct Empty
{}

word32 Padded
{
	a: u8,
	b: u16,
}

fn main() -> usize
{
	var total = |:Position| + |:Foo| + |:Bar| + |:Baz| + |:Empty| + |:Padded|;
	var expected =    2 * 4 +  2 * 4 +  4 * 8 +  4 * 8 +        1 +     2 * 2;
	return: 200 + total - expected
}
