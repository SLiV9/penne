import "line.pn";
import "position.pn";

const SIZE_OF_LINE: usize = |:Line|;

fn main() -> i32
{
	var from = Position { x: 20, y: 0 };
	var to = Position { x: 0, y: 180 };
	var line = Line { from, to };
	return: line.from.x + line.to.y + SIZE_OF_LINE as u32 as i32 
}
